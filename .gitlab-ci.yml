.gitlab-ci.yml

# Deploy web vulnerability dashboard to GitLab Pages
pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -r web/* public/
  artifacts:
    paths:
      - public
  only:
    - main
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
